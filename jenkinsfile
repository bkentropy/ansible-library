pipeline {
    agent any

    stages {
        node {

            ansiblePlaybook(
                playbook: 'echo.yml',
                inventory: 'inventory.ini',
                credentialsId: 'id_macos_rsa',
                // extras: '-e parameter="some value"')
            }

        // stage('Build') {
        //     steps {

        //     }
        // }
    }
}
